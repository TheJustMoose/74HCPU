!!!!спаять уже всё нахрен!!!!

блин! я же совсем забыл о том, что стек то у меня есть,
 а вот счётчиков/мультиплексоров для выбора, какую ячейку читать/писать,
 я так и не дорисовал на плате
 (и триггер для фиксации stack overflow тоже нужен)

переделать MUX8:1 1bit, поменять порядок входов D0...D7
 ну и разъёмы из середины перенести в торец, чтобы избавиться от проводов

сделать торцевую плату для однобитных мультиплексоров

сделать плату для рубильников (на 8 бит)

сделать новый вариант регистра-счётчика на 8+ бит
 (хотя бы на 9, или сразу на 12, иначе выводить видео хз как)

поставить хотя бы один эксперимент с gLCD,
 например вывести на него ОДИН одноцветный прямоугольник на всю его площадь ;)

поиграть с Dual Port Video RAM

начать рисовать плату для gLCD
 (накидать туда хотя бы разъём gLCD, счётчики, генераторы)

переделать MUX2:1 так, чтобы оба входа располагались не по горизонтали, а по вертикали
         H_in     L_in     Out
 было  ........ ........ ........

         H_in     Out
 стало ........ ........
       ........
         L_in

купить запас мультиплексоров

проверить, что разъёмы плат удобно расположены

переделать плату ROM, чтобы туда намертво запаивался ZIF сокет,
 и не нужно было втыкать в разъёмы на плате переходник ZIF -> DIP,
 а уже в переходник саму ROM

начать рисовать прерывания
предусмотреть два регистра флагов - для обычной работы и работы в ISP

попробовать поковырять UART0, для начала хотя бы в виде микросхемы + Arduino!

придумать что-то с таблицей виртуальных методов,
 например сделать ICALL, или RCALL, или хз,
 как вызвать код по индексу в таблице, которая лежит в ROM
 в общем, проверить как-то, что у меня заведено в мультиплексор адреса
 (и какой вообще адрес он мультиплексирует)

условно: есть некая таблица, которая лежит в ROM
в ней есть:
addr0 - адрес виртуального метода 0
addr1 - адрес виртуального метода 1
.....

таких табличек много
адрес таблички, видимо, должен лежать в ...? в RAM, рядом со всеми данными объекта класса?
или где-то в ROM? ведь адрес таблицы не меняется после компиляции...
хотя, если есть наследование, то адрес как раз должен меняться
Оо

в любом случае, чтобы вызвать функцию/метод по индексу в табличке, можно сделать:
 берём адрес начала таблицы,
 прибавляем к нему индекс,
 читаем из этого адреса 16 бит,
 считаем эти 16 бит адресом, вызываем метод по этому адресу

альтернатива:
 располагаем перед таблицей код, который делает CALL, как раз по смещению, относительно себя
 .... а толку?

а как должен работать ICALL? брать адрес из какого-нибудь регистра?

кстати, в таблице можно располагать не абсолютные адреса,
 а саму команду CALL... и делать на эту команду JMP
  вот только не понятно, как это сделать :(
